package com.simon.secondtest

/**
 i moved all my test to the instrumentation tests directory because i need to also test data store, which needs
 access to testcontext, check the other test directory for my test cases, thanks
 */